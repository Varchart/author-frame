package com.author.commons.utils.enums;

public enum Rc {
	appid, quid, qticket, data, cookie, ftime, code, client_id, access_token, refresh_token, campaign_id, campaign_name, campaign_type, is_deleted, account_id, cost, date_range,
	level, group_by, fields, timestamp, page, page_size, nonce, list, secret;
}
